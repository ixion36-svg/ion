# =============================================================================
# ION - Docker Compose with Elasticsearch Logging
# =============================================================================
# Includes Filebeat sidecar for shipping logs to Elasticsearch.
#
# Usage:
#   docker compose -f docker-compose.elk.yml up -d
#
# Prerequisites:
#   - Elasticsearch cluster accessible at ELASTICSEARCH_HOSTS
#   - Run setup-elasticsearch.sh first to create indices/policies
# =============================================================================

services:
  # ===========================================================================
  # ION Application
  # ===========================================================================
  ion:
    image: ixion36/ion:${ION_VERSION:-latest}
    container_name: ion
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      # Logging settings
      - ION_LOG_LEVEL=${LOG_LEVEL:-INFO}
      - ION_LOG_JSON=true
      - ION_LOG_FILE=/var/log/ion/app.log

      # Environment identifier for logs
      - ENVIRONMENT=${ENVIRONMENT:-production}
    volumes:
      # Persistent data
      - ion-data:/data
      # Shared log volume with Filebeat
      - ion-logs:/var/log/ion
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - ion-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ===========================================================================
  # Filebeat - Log Shipper
  # ===========================================================================
  filebeat:
    image: docker.elastic.co/beats/filebeat:8.11.0
    container_name: ion-filebeat
    restart: unless-stopped
    user: root
    environment:
      # Elasticsearch connection
      - ELASTICSEARCH_HOSTS=${ELASTICSEARCH_HOSTS:-["http://elasticsearch:9200"]}
      - ELASTICSEARCH_USERNAME=${ELASTICSEARCH_USERNAME:-}
      - ELASTICSEARCH_PASSWORD=${ELASTICSEARCH_PASSWORD:-}
      - ELASTICSEARCH_SSL_ENABLED=${ELASTICSEARCH_SSL_ENABLED:-false}

      # Kibana for dashboards (optional)
      - KIBANA_HOST=${KIBANA_HOST:-http://kibana:5601}

      # Environment
      - ENVIRONMENT=${ENVIRONMENT:-production}

      # Monitoring
      - MONITORING_ENABLED=${MONITORING_ENABLED:-false}
    volumes:
      # Filebeat configuration
      - ./filebeat/filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      # ION logs
      - ion-logs:/var/log/ion:ro
      # Filebeat data (registry, etc.)
      - filebeat-data:/usr/share/filebeat/data
      # Docker socket for container metadata
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: ["--strict.perms=false"]
    depends_on:
      ion:
        condition: service_healthy
    networks:
      - ion-net
    healthcheck:
      test: ["CMD", "filebeat", "test", "config"]
      interval: 30s
      timeout: 10s
      retries: 3

# =============================================================================
# Networks
# =============================================================================
networks:
  ion-net:
    driver: bridge

# =============================================================================
# Volumes
# =============================================================================
volumes:
  ion-data:
    name: ion-data
  ion-logs:
    name: ion-logs
  filebeat-data:
    name: ion-filebeat-data
